# Variabilele de mediu ale stack-ului din Portainer (sau .env lângă
# docker-compose.yml dacă rulezi docker compose direct).
# Generează cheia cu: openssl rand -hex 32
API_KEY=
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=becash
MYSQL_PASSWORD=
MYSQL_DB=becash_player
